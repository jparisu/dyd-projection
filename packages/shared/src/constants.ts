export const DEFAULT_GRID_SIZE = 50; // pixels per cell
export const DEFAULT_VISION_RADIUS = 6; // cells
export const DEFAULT_SERVER_PORT = 4000;
export const DEFAULT_WEB_PORT = 5173;

/** Demo session bootstrapped in-memory by the server until persistence lands. */
export const DEMO_SESSION_ID = 'demo';
